.venv/
__pycache__/
*.pyc
.env
.claude/
data/*.db
data/generated/
.DS_Store
data/chat/roster.json
data/email/vendors.json

# Bedrock AgentCore deploy artifacts (contain AWS account id / IAM role ARN)
/.bedrock_agentcore.yaml
/Dockerfile
/.dockerignore

# The atomic-write staging file for the decisions cache; only ever transient,
# but a crash between write and rename must not dirty the tree.
/data/synthetic/decisions.json.tmp
